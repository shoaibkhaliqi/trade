# Full backtest profile: complete historical periods, stricter accounting.
mode: backtest
seed: 42

data:
  symbol: XAUUSDT
  timeframes: ["1m", "5m", "15m", "1h"]
  source: bybit
  category: linear       # XAUUSDT is a USDT-perpetual (tokenized gold), not spot
  raw_dir: data/raw
  processed_dir: data/processed
  features_dir: data/features
  # null => everything the exchange has (XAUUSDT lists 2026-03-09 on Bybit)
  days_back:
    "1m": null
    "5m": null
    "15m": null
    "1h": null

splits:
  train: null            # set at M7, e.g. ["2021-01-01", "2024-12-31"]
  validation: null       # e.g. ["2025-01-01", "2025-12-31"]
  test: null             # e.g. ["2026-01-01", null]; NEVER used for training

simulator:
  initial_capital: 1000.0
  taker_fee_pct: 0.055
  slippage_pct: 0.02
  fixed_position_size_pct: 25.0

risk:
  max_position_size_pct: 50.0
  max_leverage: 1.0
  max_daily_loss_pct: 5.0

experiment:
  sqlite_path: experiments/metadata.sqlite
